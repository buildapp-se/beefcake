node_modules
dist
dist-ssr
*.local
.env
.env.*
*.log
npm-debug.log*
.DS_Store
*.swp
*.swo
.vscode
.idea
*.tsbuildinfo
nul
.playwright-shots
.dev.vars

# graft's local graph cache â€” regenerable, not committed (run `graft build`).
graft/

# Wrangler's local state, generated bundle and generated type declarations.
.wrangler/
server/dist/
server/worker-configuration.d.ts

# repowise local index - regenerable, not committed (run `repowise update`).
.repowise/

# Playwright MCP session-artefakter, regenereras vid varje körning.
.playwright-mcp

# Skärmbilder från webbläsarverifiering hamnar i repotroten, de ska aldrig committas.
/*.png

# Firebase Hosting deploy-cache i authhost, regenereras vid deploy.
authhost/.firebase/
